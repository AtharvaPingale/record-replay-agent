{
  "artifact_id": "vb-bank-admin-balance",
  "version": 2,
  "target": {
    "surface": "web",
    "base_url_pattern": "https://vb-bank-demo.vercel.app/*",
    "entry_url": "https://vb-bank-demo.vercel.app/login"
  },
  "input_schema": {
    "account_number": "string"
  },
  "output_schema": {
    "account_balance": "string"
  },
  "checkpoint": {
    "type": "selector_visible",
    "value": "text={{account_number}}",
    "only_when_url_contains": null
  },
  "business_outcomes": [
    {
      "name": "account_not_found",
      "match": {
        "type": "text_present",
        "value": "No users found",
        "only_when_url_contains": null
      },
      "alert_human": false
    }
  ],
  "recoverable_patterns": [],
  "output_extractors": [
    {
      "name": "account_balance",
      "target": {
        "kind": "dom_selector",
        "value": "xpath=//*[normalize-space(text())='{{account_number}}']/following-sibling::*[1]"
      }
    }
  ],
  "steps": [
    {
      "step": 1,
      "action": "click",
      "target": {
        "primary": {
          "kind": "dom_selector",
          "value": "[data-testid=\"btn-quick-login-admin\"]"
        },
        "fallback": null,
        "robustness_reasoning": "Stable data-testid attribute for the admin quick-login button, present in the app's DOM specifically for testing/demo purposes; more robust than relying on visible text alone."
      },
      "params": {},
      "idempotency_note": "Safe, no-op if already logged in as admin -- would just navigate again or be a no-op click on a stale page.",
      "max_recovery_attempts": null
    },
    {
      "step": 2,
      "action": "click",
      "target": {
        "primary": {
          "kind": "dom_selector",
          "value": "[data-testid=\"nav-link-user-management\"]"
        },
        "fallback": null,
        "robustness_reasoning": "Stable data-testid on the sidebar nav link that routes to /admin/users; this is a persistent navigation element in the admin layout, not content that changes between runs."
      },
      "params": {},
      "idempotency_note": "Safe, no-op if already on User Management page -- clicking again just re-navigates to the same route.",
      "max_recovery_attempts": null
    },
    {
      "step": 3,
      "action": "type",
      "target": {
        "primary": {
          "kind": "dom_selector",
          "value": "#this-control-no-longer-exists"
        },
        "fallback": null,
        "robustness_reasoning": "Stable data-testid on the search input, explicitly labeled as supporting search by account number; this is a persistent form control on the User Management page, not content that varies between runs."
      },
      "params": {
        "text": "{{account_number}}"
      },
      "idempotency_note": "Safe, no-op if already typed -- re-typing the same value into the search field just re-triggers the same filter.",
      "max_recovery_attempts": null
    },
    {
      "step": 4,
      "action": "wait",
      "target": null,
      "params": {
        "ms": 1000
      },
      "idempotency_note": "side-effect-free",
      "max_recovery_attempts": null
    }
  ],
  "revision_note": "NOT A REAL VERSION -- --demo-break-step corrupted step 3's locators (originally dom_selector '[data-testid=\"input-search-users\"]') to force a hard_failure."
}